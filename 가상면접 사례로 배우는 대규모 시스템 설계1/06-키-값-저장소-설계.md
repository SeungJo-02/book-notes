# 6장. 키-값 저장소 설계

> 📝 **Notion 원본**: https://app.notion.com/p/2318b2a22e2e4e0fb959a15ddcefb30f

> 🧩 **한 줄 요약** — 대규모 분산 키-값 저장소(다이나모·카산드라형). **CAP에서 파티션 감내는 필수**라 실무에선 일관성(CP) vs 가용성(AP) 중 택한다. 데이터는 **안정 해시로 파티션** → **N개 노드에 복제**하고, 일관성은 **정족수(W+R>N)**로 조율, 사본 충돌은 **벡터 시계**로, 장애는 **가십·단서 후 임시 위탁·머클 트리**로 처리한다. 저장 엔진은 쓰기=커밋로그→메모리→SSTable, 읽기=블룸 필터.

---

## 목차
1. [키-값 저장소란 & 요구사항](#1-키-값-저장소란--요구사항)
2. [단일 서버 → 분산, 그리고 CAP](#2-단일-서버--분산-그리고-cap)
3. [데이터 파티션 & 다중화 — 안정 해시](#3-데이터-파티션--다중화--안정-해시)
4. [데이터 일관성 — 정족수(N·W·R) ⭐](#4-데이터-일관성--정족수nwr-)
5. [충돌 해소 — 벡터 시계](#5-충돌-해소--벡터-시계)
6. [장애 처리](#6-장애-처리)
7. [아키텍처 & 읽기/쓰기 경로](#7-아키텍처--읽기쓰기-경로)
8. [정리 & 셀프 체크](#정리)

---

## 1. 키-값 저장소란 & 요구사항

- **키-값 저장소** = 키-값 DB라고도 불리는 **NoSQL**. 키는 **고유 식별자**로 유일해야 하고, 값은 **키를 통해서만** 접근 가능.
- 키는 **짧을수록 성능↑**, 일반 텍스트/해시 값 가능. 값은 문자열·리스트·객체 가능.
- **요구사항** — 키-값 쌍 **10KB 이하** · 큰 데이터 저장 · **높은 가용성**(장애에도 빠른 응답) · **높은 확장성**(자동 증설/삭제) · **조정 가능한 일관성** · 짧은 응답 지연.

---

## 2. 단일 서버 → 분산, 그리고 CAP

- **단일 서버**: 전부 메모리 해시 테이블 → 빠르지만 **메모리 한계**. 개선책은 **데이터 압축 + 자주 쓰는 것만 메모리, 나머지 디스크**. 그래도 언젠가 **분산 저장소**가 필요.

### CAP 정리
세 가지를 **동시에 만족 불가** → 둘 고르면 하나는 희생.

| | 뜻 |
|---|---|
| **C** 일관성 | 어느 노드에 접속해도 **항상 같은 데이터** |
| **A** 가용성 | 일부 노드 장애에도 **항상 응답** |
| **P** 파티션 감내 | 노드 간 통신 장애(파티션)가 나도 **계속 동작** |

- **CP**(일관성+P, 가용성 희생) / **AP**(가용성+P, 일관성 희생) / **CA**(일관성+가용성, P 희생).
- 실세계 네트워크 장애는 피할 수 없으니 **P는 필수** → **CA 시스템은 실세계에 없다**. 결국 **CP냐 AP냐**의 선택.

> 🔑 예: n3 노드가 끊긴 상황 — **CP는 불일치를 막으려 쓰기를 중단**(가용성↓), **AP는 낡은 데이터를 감수하고 계속 읽기 허용** 후 복구되면 동기화.

---

## 3. 데이터 파티션 & 다중화 — 안정 해시

### 3-1. 데이터 파티션
- 데이터를 작은 파티션으로 쪼개 여러 서버에 저장. 고려점: **고른 분산** + **노드 추가/삭제 시 이동 최소화** → 해결책은 **안정 해시**.
- 서버를 **해시 링**에 배치, 키를 링에 올린 뒤 **시계 방향 첫 서버**에 저장.
- 장점: **규모 확장 자동화**(부하 따라 서버 자동 추가/삭제) · **다양성**(고성능 서버엔 가상 노드를 더 많이 배정).

### 3-2. 데이터 다중화(복제)
- 가용성·안정성 위해 데이터를 **N개 서버에 비동기 복제**. 키를 링에 올리고 시계 방향으로 만나는 **첫 N개 서버**에 사본 보관(N=3이면 s1·s2·s3).
- 주의: 가상 노드 때문에 **같은 물리 서버 중복 선택 금지**, 사본은 **다른 데이터센터**에 두고 고속망으로 연결.

---

## 4. 데이터 일관성 — 정족수(N·W·R) ⭐

**정족수 합의(Quorum Consensus)** — 읽기/쓰기 일관성을 조율.

| 기호 | 뜻 |
|---|---|
| **N** | 사본 개수 |
| **W** | 쓰기 성공으로 간주할 **최소 성공 응답 수** |
| **R** | 읽기 성공으로 간주할 **최소 응답 수** |

- W, R이 **작으면 빠르지만** 강한 일관성 X / **크면 일관성↑ 느림**. 응답 지연 ↔ 일관성의 **타협점**.

| 설정 | 성격 |
|---|---|
| `R=1, W=N` | 빠른 **읽기** 최적화 |
| `W=1, R=N` | 빠른 **쓰기** 최적화 |
| **`W+R > N`** | **강한 일관성 보장** (보통 N=3, W=R=2) |
| `W+R ≤ N` | 강한 일관성 미보장 |

### 일관성 모델
- **강한 일관성**: 읽기는 항상 최신값. **약한 일관성**: 최신값 아닐 수 있음. **결과적 일관성**: 약한 일관성의 형태로 **결국 모든 사본에 반영**(다이나모·카산드라가 채택).

---

## 5. 충돌 해소 — 벡터 시계

- 다중화하면 가용성↑ 대신 **사본 간 충돌** 위험↑. **버저닝**(변경마다 새 불변 버전 생성)으로 대응.
- 동시에 서로 다른 값으로 바꾸면 충돌(v1 vs v2) → **벡터 시계(vector clock)**로 해결.
  - 데이터에 **`[서버, 버전]` 순서쌍**을 매달아, 어떤 버전이 **선행/후행/충돌**인지 판별.
  - 예: `D3([Sx,2],[Sy,1])`와 `D4([Sx,2],[Sz,1])`를 함께 읽으면 **충돌 감지** → 클라이언트가 해소 후 `D5([Sx,3],[Sy,1],[Sz,1])`로 기록.
- **단점**: ① 충돌 해소 로직이 **클라이언트로** 가서 구현 복잡 ② `[서버:버전]` **순서쌍이 빠르게 증가**(임계치 넘으면 오래된 것 제거 → 선후 관계 부정확해지나 실제 문제는 관측 안 됨).

---

## 6. 장애 처리

### 6-1. 장애 감지 — 가십 프로토콜
- 전 노드를 잇는 **멀티캐스팅**은 쉽지만 서버 많으면 비효율.
- **가십(gossip)**: 각 노드가 **박동 카운터**를 주기적으로 올리고, 무작위 노드에 멤버십 목록을 전파. **일정 시간 카운터 갱신 없으면 장애**로 간주.

### 6-2. 일시적 장애 — 단서 후 임시 위탁(hinted handoff)
- 장애 노드로 갈 요청을 **다른 서버가 임시 처리**하고 **단서(hint)**를 남김. 노드 복구 시 **변경분을 일괄 인계**(예: s2 대신 s3가 처리 → s2 복구되면 s3가 인계).

### 6-3. 영구 장애 — 반-엔트로피 & 머클 트리
- **반-엔트로피(anti-entropy)**: 사본을 비교해 최신으로 동기화.
- **머클(Merkle) 트리**: 키 공간을 **버킷**으로 나눠 해시 → 상향식 이진 트리 구성. **루트 해시부터 비교**해 내려가며 **다른 버킷만** 찾아 동기화(전송량↓).

### 6-4. 데이터센터 장애
- **여러 데이터센터에 다중화** → 한 센터가 죽어도 다른 센터 데이터 사용.

---

## 7. 아키텍처 & 읽기/쓰기 경로

- **API**: `get(key)`, `put(key, value)` 2개.
- **중재자(coordinator)**: 클라이언트-저장소 사이 **프락시** 노드. 노드는 해시 링에 분포, **완전 분산**이라 **SPOF 없음**(모든 노드가 같은 책임).

### 쓰기 경로 (카산드라 사례)
1. **커밋 로그**에 기록 → 2. **메모리 캐시**에 기록 → 3. 캐시가 차면 디스크의 **SSTable**(정렬된 <키,값> 리스트)에 기록.

### 읽기 경로
- 메모리에 있으면 바로 반환. 없으면 → **블룸 필터**로 어느 SSTable에 있는지 찾고 → SSTable에서 가져와 반환.

---

## 정리

| 기능 | 사용 기술 |
|---|---|
| 대규모 데이터 저장 | 안정 해시로 **파티션** |
| 읽기 고가용성 | **다중화**(여러 데이터센터) |
| 쓰기 고가용성 | **벡터 시계**로 충돌 해소 |
| 일관성 수준 | **정족수(N·W·R)** 조율 |
| 일시 장애 | 단서 후 임시 위탁 |
| 영구 장애 | 머클 트리(반-엔트로피) |
| 장애 감지 | 가십 프로토콜 |
| 저장 엔진 | 커밋로그·SSTable·블룸 필터 |

## 🔁 셀프 체크
1. CAP에서 실세계 분산 시스템은 왜 사실상 **CP냐 AP냐**의 선택이 되나?
2. `W+R > N`이 강한 일관성을 보장하는 이유는? (N=3, W=R=2 예로)
3. 영구 장애 동기화에서 **머클 트리**가 전송량을 줄이는 원리는?

<details>
<summary>답</summary>

1. 실세계 네트워크 장애(파티션)는 피할 수 없어 **P는 필수**다. P를 버린 CA 시스템은 성립하지 않으므로, 남은 C와 A 중 하나를 택하는 문제가 된다.
2. 쓴 노드 집합(W)과 읽는 노드 집합(R)이 **반드시 겹치기** 때문. W=R=2, N=3이면 2+2>3이라 읽을 때 최소 한 노드는 **가장 최근 쓰기를 본 노드**와 겹쳐 최신값을 보장한다.
3. **루트 해시부터 비교**해 같으면 전체 동일로 끝내고, 다르면 자식으로 내려가며 **차이 나는 버킷만** 찾아낸다. 그래서 전체가 아니라 **어긋난 부분만** 동기화한다.

</details>

---

<details>
<summary>📄 원본 노트 (정리 전 원문)</summary>

- 키-값 저장소: 키-값 DB라고도 불리는 NoSQL
- 저장소 속 키 값은 고유 식별자
- 키-값 사이의 연결 관계를 `키-값` 쌍
- 키는 유일해야하고 키에 메달린 값은 키를 통해서면 접근 가능
- 키는 짧을 수록 성능에 좋다
- 키는 일반 텍스트일 수도, 해시 값일 수도
- `값`은 문자열일 수도, 리스트일 수도, 객체일 수도

**문제 이해 및 설계 범위 확정** — 키-값 저장소의 특성
- 키-값 쌍의 크기는 10KB 이하
- 큰 데이터를 저장할 수 있어야 함
- 높은 가용성 제공(시스템에 장애가 생겨도 빨리 응답)
- 높은 규모 확장성 제공(트래픽 양에 따라 자동적으로 서버 증설/삭제가 이뤄지게)
- 데이터 일관성 수준은 조정이 가능해야 함
- 응답 지연시간이 짧아야 함

**단일 서버 키-값 저장소**
- 키-값 쌍 전부를 메모리에 해시 테이블로 저장하는 방법은 가장 직관적
	- 빠른 속도 보장
	- 모든 데이터를 메모리 안에 두는 것이 불가능할 수 있음
- 위 문제를 해결하기 위한 개선책
	- 데이터 압축
	- 자주 쓰이는 데이터만 메모리에 두고 나머지는 디스크로
- 언젠간 서버 한 대는 부족해진다. 나중엔 언젠간 분산 키-값 저장소 필요

**CAP (Consistency, Availability, Partition Tolerance theorem)**
- 데이터 일관성, 가용성, 파티션 감내라는 3가지 요구사항을 동시에 만족하는 분산 시스템을 설계할 수는 없다
	- 데이터 일관성: 분산 시스템에 접속하는 모든 클라이언트는 어떤 노드에 접속했느냐에 관계없이 언제나 같은 데이터를 보도록
	- 가용성: 분산 시스템에 접속하는 클라이언트는 일부 노드에 장애가 발생해도 항상 응답을 받을 수 있게
	- 파티션 감내: 파티션은 두 노드 사이에 통신 장애가 발생한 것. 즉, 네트워크에 파티션이 생기더라도 시스템은 계속 동작해야 해야하는 것
- CAP 정리
	- 어떤 2가지를 충족하려면 나머지 하나는 반드시 희생되어야 한다.
	- CP 시스템: 일관성과 파티션 감내를 지원하는 키-값 저장소. 가용성을 희생
	- AP 시스템: 가용성과 파티션 감내를 지원하는 키-값 저장소. 데이터 일관성을 희생
	- CA 시스템: 일관성과 가용성을 지원하는 키-값 저장소. 파티션 감내는 지원하지 않음
	- 통상 네트워크 장애는 피할 수 없으므로, 분산 시스템은 파티션 문제를 반드시 감내할 수 있어야 한다. (실세계에 CA 시스템 존재X)

*이상적 상태*
- 분산 시스템에서 데이터는 보통 여러 노드에 복제되어 보관된다. 3대의 복제(replica) 노드 n1, n2, n3에 데이터를 보관하는 상황
- 이상적 환경이라면 네트워크가 파티션되는 상황은 절대로 일어나지 않음
- n1에 기록된 데이터는 자동적으로 n2와 n3에 복제된다. 데이터 일관성과 가용성도 만족

*실세계의 분산 시스템*
- 분산 시스템은 파티션 문제를 피할 수 없다. (문제가 발생하면 일관성과 가용성 중 하나 선택이 필요). n3에 장애가 발생하여 n1 및 n2와 통신할 수 없는 상황
- 클라이언트가 n1 또는 n2에 기록한 데이터는 n3에 전달되지 않는다.
- n3에 기록되었으니 아직 n1 및 n2로 전달되지 않은 데이터가 있다면 n1과 n2는 오래된 사본을 갖고 있을 것이다.
- CP 시스템 (일관성 선택): 데이터 불일치를 피하기 위해 n1과 n2에 대해 쓰기 연산을 중단→가용성이 깨짐
- AP 시스템 (가용성 선택): 낡은 데이터를 반환할 위험이 있더라도 계속 읽기 연산을 허용→ 문제 해결 후 새 데이터를 n3에 전송
- 분산 키-값 저장소를 만들 때는 요구사항에 맞도록 CAP 정리 적용

**시스템 컴포넌트**

*데이터 파티션*
- 데이터를 작은 파티션들로 분할한 다음 여러 대 서버에 저장
- 파티션 단위로 나눌 때 고려해야 하는 거
	- 데이터를 여러 서버에 고르게 분산할 수 있는지
	- 노드가 추가되거나 삭제될 때 데이터의 이동을 최소화할 수 있는지
- 해결책은 안정 해시
	- 서버를 해시 링에 배치
	- 어떤 키-값 쌍을 어떤 서버에 저장할지 결정하기 위해 우선 해당 키를 같은 링 위에 배치. 그 지점으로부터 링을 시계 방향으로 순회하다 만나는 첫번째 서버가 해당 키-값 쌍을 저장할 서버. 따라서 key0은 s1에 저장된다.
- 안정 해시를 사용하여 데이터를 파티션했을 때 좋은 점
	- 규모 확장 자동화: 시스템 부하에 따라 서버가 자동으로 추가되거나 삭제되도록 만들 수 있음
	- 다양성: 각 서버의 용량에 맞게 가상 노드의 수를 조정할 수 있음 (고성능 서버는 더 많은 가상 노드를 갖도록 설정 가능)

*데이터 다중화*
- 높은 가용성과 안정성을 확보하기 위해 데이터를 N개 서버에 비동기적으로 다중화할 필요가 있다
- N개 서버를 선정하는 방법: 어떤 키를 해시 링 위에 배치하고, 그 지점으로부터 시계 방향으로 링을 순회하면서 만나는 첫 N개 서버에 데이터 사본을 보관
- N=3일 경우: key0은 s1, s2, s3에 저장된다.
- 가상 노드를 사용한다면 선택한 N개의 노드가 대응될 실제 물리 서버의 개수가 N보다 작아질 수 있다
	- 노드를 선택할 때 같은 물리 서버를 중복 선택하지 않도록 한다
	- 안정성을 담보하기 위해 데이터의 사본은 다른 센터의 서버에 보관하고, 센터들은 고속 네트워크로 연결한다

*데이터 일관성*
- 여러 노드에 다중화된 데이터는 적절히 동기화가 되어야 한다
- 정족수 합의(Quorum Consensus) 프로토콜을 사용하면 읽기/쓰기 연산 모두에 일관성을 보장할 수 있다
	- N: 사본 개수
	- W: 쓰기 연산에 대한 정족수 (쓰기 연산이 성공한 것으로 간주될 때 필요한 최소 성공 응답 개수)
	- R: 읽기 연산에 대한 정족수 (읽기 연산이 성공한 것으로 간주될 때 필요한 최소 서버 응답 개수)
- N=3인 경우
	- W=1: 쓰기 연산이 성공했다고 판단하기 위해 중재자는 최소 한 대 서버로부터 쓰기 성공 응답을 받아야 한다. s1으로부터 성공 응답을 받았다면, s0과 s2로부터의 응답은 기다릴 필요 없다
- W, R, N의 값을 정하는 것은 응답 지연과 데이터 일관성 사이의 타협점을 찾는 과정이다.
	- W과 R의 값이 작다면, 응답 속도는 빠르다. (강한 데이터 일관성이 보장되지 않음)
	- W과 R의 값이 크다면, 데이터 일관성의 수준이 향상된다. (응답 속도는 느림)
- N, W, R 값은 어떻게 정할 수 있을까?
	- R=1, W=N: 빠른 읽기 연산에 최적화된 시스템
	- W=1, R=N: 빠른 쓰기 연산에 최적화된 시스템
	- W+R > N: 강한 일관성이 보장됨 (보통 N=3, W=R=2)
	- W+R < N: 강한 일관성이 보장되지 않음

*일관성 모델: 데이터 일관성의 수준 결정*
- 강한 일관성: 모든 읽기 연산은 가장 최근에 갱신된 결과를 반환한다.
- 약한 일관성: 읽기 연산은 가장 최근에 갱신된 결과를 반환하지 못할 수 있다.
- 결과적 일관성: 약한 일관성의 한 형태로, 갱신 결과가 결국에는 모든 사본에 반영(동기화)되는 모델이다.

*비 일관성 해소 기법: 데이터 버저닝*
- 데이터를 다중화하면 가용성은 높아지지만, 사본 간 일관성이 깨질 가능성도 높아짐
- 버저닝: 데이터를 변경할 때마다 해당 데이터의 새로운 버전을 만드는 것. 각 버전의 데이터는 변경 불가능
- 데이터의 사본이 n1과 n2에 보관된 예제: 데이터를 가져오려는 서버1과 서버2는 get("name") 연산의 결과로 같은 값을 얻는다.
- 데이터의 일관성이 깨지는 예제: 서버1은 "name"에 매달린 값을 "johnSanFrancisco"로 바꾸고, 서버2는 "johnNewYork"으로 변경했다. (두 연산은 동시에 발생). 충돌(conflict)하는 두 값을 갖게 되었다. 각각의 버전은 v1, v2라고 하자.
- 백터 시계(vector clock) 라는 버저닝 시스템을 통해 충돌을 해결한다.
	- [서버, 버전]의 순서쌍을 데이터에 매단 것이다.
	- 어떤 버전이 선행 버전인지, 후행 버전인지, 아니면 다른 버전과 충돌이 있는지 판별하는 데 쓰인다.
- 백터 시계의 수행 로직
	1. 클라이언트가 데이터 D1을 시스템에 기록한다. 이 쓰기 연산을 처리한 서버는 Sx이다. 따라서 백터 시계는 D1[Sx, 1]으로 변한다.
	2. 다른 클라이언트가 데이터 D1을 읽고 D2로 업데이트한 후 기록한다. D2는 D1에 대한 변경이므로 D1을 덮어쓴다. 이때 쓰기 연산은 같은 서버 Sx가 처리한다고 가정한다. 백터 시계는 D2([Sx, 2])로 바뀐다
	3. 다른 클라이언트가 D2를 읽어 D3로 갱신한 다음 기록한다. 이 쓰기 연산은 Sy가 처리한다고 가정하자. 백터 시계 상태는 D3([Sx, 2], [Sy, 1])로 바뀐다
	4. 또 다른 클라이언트가 D2를 읽고 D4로 갱신한 다음 기록한다. 이때 쓰기 연산은 Sz가 처리한다고 가정하자. 백터 시계는 D4([Sx, 2], [Sz, 1])가 된다
	5. 어떤 클라이언트가 D3과 D4를 읽으면 데이터 간 충돌이 있다는 것을 알게 된다. 이 충돌은 클라이언트가 해소한 후에 서버에 기록한다. 이 쓰기 연산을 처리한 서버는 Sx였다고 하자. 백터 시계는 D5([Sx, 3], [Sy, 1], [Sz, 1])로 바뀐다
- 백터 시계의 단점
	- 충돌 감지 및 해소 로직이 클라이언트에 들어가야 하므로, 클라이언트 구현이 복잡해짐
	- [서버: 버전]의 순서쌍 개수가 굉장히 빨리 늘어남(임계치 이상으로 길이가 길어지면 오래된 순서쌍을 벡터 시계에서 제거하도록 한다. 이렇게 되면 버전 간 선후 관계가 정확하지 않아 충돌 해소 과정의 효율성이 낮아진다. 하지만 실제 서비스에서 관련 문제가 발생하는 것은 발견되지 않았다고 한다.)

*장애 처리*
- 모든 노드 사이에 멀티캐스팅 채널 구축(전부 잇는 방법)
	- 가장 손쉬운 방법이다.
	- 서버가 많을 때는 비효율적
- 가십 프로토콜: 분산형 장애 감지 솔루션, 효율적
	- 각 노드는 멤버십 목록을 유지한다. 멤버십 목록은 각 멤버 ID와 그 박동 카운터 쌍의 목록
	- 각 노드는 주기적으로 자신의 박동 카운터를 증가시킨다
	- 각 노드는 무작위로 선정된 노드들에게 주기적으로 자기 박동 카운터 목록을 보내고
	- 박동 카운터 목록을 받은 노드는 멤버십 목록을 최신 값으로 갱신
	- 어떤 멤버의 박동 카운터 값이 지정된 시간 동안 갱신되지 않으면 해당 멤버는 장애상태인 것으로 간주
- 일시적 장애 처리
	- 장애 상태인 서버로 가는 요청은 다른 서버가 잠시 맡아 처리한다.
	- 그동안 발생한 변경사항은 해당 서버가 복구되었을 때 일괄 반영하여 데이터 일관성을 보존한다.
	- 이를 위해 임시로 쓰기 연산을 처리한 서버에는 그에 관한 단서(hint)를 남겨둔다. (단서 후 임시 위탁)
	- 단서 후 임시 위탁 예제: 장애 상태인 노드 s2에 대한 읽기 및 쓰기 연산은 일시적으로 노드 s3가 처리한다. s2가 복구되면, s3는 갱신된 데이터를 s2로 인계한다.
- 영구 장애 처리
	- 영구적인 노드의 장애 상태를 처리하기 위해 반-엔트로피(anti-entropy) 프로토콜을 구현하여 사본들을 동기화한다.
	- 반-엔트로피 프로토콜: 사본들을 비교하여 최신 버전으로 갱신하는 과정을 포함한다.
	- 머클(Merkle) 트리: 사본 간의 일관성이 망가진 상태를 탐지하고 전송 데이터의 양을 줄이기 위해 사용한다.
	- 키 공간(key space)이 1부터 12까지일 때 머클 트리를 만드는 예제
		1. 키 공간을 버킷(bucket)으로 나눈다.
		2. 버킷에 포함된 각각의 키에 균등 분포 해시(uniform hash) 함수를 적용하여 해시 값을 계산한다.
		3. 버킷별로 해시값을 계산한 후, 해당 해시 값을 레이블로 갖는 노드를 만든다.
		4. 자식 노드의 레이블로부터 새로운 해시 값을 계산하여, 이진 트리를 상향식으로 구성해 나간다.
	- 두 머클 트리의 비교는 루트(root) 노드의 해시값을 비교하여 시작한다. 루트 노드의 해시 값이 일치한다면 두 서버는 같은 데이터를 갖는 것이다. 값이 다른 경우에는 왼쪽 자식 노드의 해시 값을 비교하고, 오른쪽 자식 노드의 해시 값을 비교한다. 아래쪽으로 탐색해 나가면 다른 데이터를 갖는 버킷을 찾을 수 있다. 그 버킷들만 동기화하면 된다.
- 데이터 센터 장애 처리
	- 데이터 센터 장애에 대응할 수 있는 시스템을 만들려면 데이터를 여러 데이터 센터에 다중화하는 것이 중요하다.
	- 한 데이터센터가 완전히 망가져도 사용자는 다른 데이터 센터에 보관된 데이터를 이용할 수 있다.

*시스템 아키텍처 다이어그램*
- 클라이언트는 키-값 저장소가 제공하는 2가지 단순한 API(get(key), put(key, value))와 통신한다.
- 중재자(coordinator)는 클라이언트에게 키-값 저장소에 대한 프락시(proxy) 역할을 하는 노드다.
- 노드는 안정 해시의 해시 링 위에 분포한다.
- 노드를 자동으로 추가 또는 삭제할 수 있도록 시스템은 완전히 분산된다.
- 데이터는 여러 노드에 다중화된다.
- 모든 노드가 같은 책임을 지므로, SPOF(Single Point of Failure)는 존재하지 않는다.
- 완전히 분산된 설계를 채택하였으므로, 모든 노드는 아래에 제시된 기능 전부를 지원해야 한다.

*쓰기 경로* — 쓰기 요청이 특정 노드에 전달되면 벌어지는 과정 (카산드라 사례 참고)
1. 쓰기 요청이 커밋 로그(commit log) 파일에 기록된다.
2. 데이터가 메모리 캐시에 기록된다.
3. 메모리 캐시가 가득차거나 임계치에 도달하면 데이터는 디스크에 있는 SSTable에 기록된다.
	- SSTable: (Sorted-String Table) <키, 값>의 순서쌍을 정렬된 리스트 형태로 관리하는 테이블

*읽기 경로*
- 데이터가 메모리 캐시에 있는 경우: 해당 데이터를 클라이언트에게 반환한다.
- 데이터가 메모리 캐시에 없을 때 읽기 연산이 처리되는 경로
	1. 데이터가 메모리에 있는지 검사한다. 없으면 2로 간다.
	2. 데이터가 메모리에 없으므로 블룸 필터(Bloom filter)를 검사한다.
	3. 블룸 필터를 통해 어떤 SSTable에 키가 보관되어 있는지 알아낸다.
	4. SSTable에서 데이터를 가져온다.
	5. 해당 데이터를 클라이언트에게 반환한다.

**요약** — 분산 키-값 저장소가 가져야 하는 기능과 그 기능 구현에 이용되는 기술 (원문은 표로 정리)

</details>
